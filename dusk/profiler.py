# -*- coding: utf-8 -*-

"""
profiler profiles instances, assessing their suitability for running Dusk.
"""

import logging


def run(instance_id):
    # TODO: profiling instance is out of scope for the MVP
    logging.debug("Profiling instance '{0}'".format(instance_id))
